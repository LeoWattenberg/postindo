---
import RatePage from "../../components/RatePage.astro";
import { getLocations, getRatesTo, type Location } from "../../lib/database";

export function getStaticPaths() {
  return getLocations().map((location) => ({
    params: { slug: location.slug },
    props: { location },
  }));
}

interface Props {
  location: Location;
}

const { location } = Astro.props;
const rates = getRatesTo(location.officeId);
---

<RatePage location={location} rates={rates} direction="to" />
